<?php
/**
 * @file
 * Code for the Database views feature feature.
 */

include_once 'database_views_feature.features.inc';

function database_views_feature_form_alter(&$form, &$form_state, $form_id) {
    if($form_id == "views_exposed_form"){
        if (isset($form['popular'])) {
        
        }
    }
}